# tests/.../feature/backend/protocol/

Dialect protocol conformance suites (pure expression-level).

- `test_protocol_conformance.py` — exhaustive structural conformance checks
  of ClickHouseDialect against the declared core dialect protocols: method
  presence, signature compatibility, and pairwise capability consistency
  across declared protocol mixins.
